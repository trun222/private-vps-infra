############################################
# Polyport Environment Variables
############################################

# Database connection string
# Uses the shared postgres service from ~/srv/apps/postgres
DATABASE_URL=postgresql://polyport:CHANGE_ME@postgres:5432/polyport_dev

# 32-byte base64 key for encrypting secrets
# Generate with: openssl rand -base64 32
SECRET_ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# Public URL where agents will reach the API
# This should be the public domain configured in Caddy
API_BASE_URL=https://tictac232434.duckdns.org/polyport/api
